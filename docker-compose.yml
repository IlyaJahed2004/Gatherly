services: 
 sql:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: gatherly-sql
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=${SQL_SA_PASSWORD}
    ports:
      - "14330:1433"
    volumes:
      - sql-data:/var/opt/mssql
volumes:
  sql-data: 